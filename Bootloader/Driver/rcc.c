#include \"rcc.h\"\n\n/**\n * @brief Initialize system clock to 168MHz\n * Using HSE 8MHz oscillator with PLL multiplier\n */\nvoid rcc_init(void)\n{\n    /* Enable HSE */\n    rcu_osci_on(CK_HSE);\n    while(RESET == rcu_flag_get(CK_HSE));\n    \n    /* Configure PLL: HSE / 2 * 42 = 168MHz */\n    rcu_pll_config(RCU_HXTAL, 42);\n    rcu_osci_on(CK_PLL);\n    while(RESET == rcu_flag_get(CK_PLL));\n    \n    /* Configure system clock prescalers */\n    rcu_system_clock_source_config(CK_SYS_PLL);\n    \n    /* Configure AHB, APB1, APB2 prescalers */\n    rcu_ahb_clock_div_config(RCU_AHB_CKSYS_DIV1);\n    rcu_apb1_clock_div_config(RCU_APB1_CKAHB_DIV2);\n    rcu_apb2_clock_div_config(RCU_APB2_CKAHB_DIV1);\n}\n\n/**\n * @brief Enable peripheral clock\n * @param periph: Peripheral identifier\n */\nvoid rcc_periph_clock_enable(rcu_periph_enum periph)\n{\n    rcu_periph_clock_enable(periph);\n}\n\n/**\n * @brief Disable peripheral clock\n * @param periph: Peripheral identifier\n */\nvoid rcc_periph_clock_disable(rcu_periph_enum periph)\n{\n    rcu_periph_clock_disable(periph);\n}\n"
